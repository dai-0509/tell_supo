# React実装ガイド - TellSupoプロジェクト

## 📚 目次
1. [実装の全体像](#実装の全体像)
2. [LaravelとReactの連携構造](#laravelとreactの連携構造)
3. [コンポーネント詳細解説](#コンポーネント詳細解説)
4. [重要な実装パターン](#重要な実装パターン)
5. [トラブルシューティングから学んだこと](#トラブルシューティングから学んだこと)

---

## 実装の全体像

### アーキテクチャ概要
```
Laravel (バックエンド)
    ↓
Blade Templates (HTML生成)
    ↓
React Components (動的UI)
    ↓
Ajax通信 (データ送受信)
```

### ファイル構成
```
resources/
├── js/
│   ├── app.tsx                 # Reactのエントリーポイント
│   └── components/
│       ├── CustomerModal.jsx   # 顧客登録モーダル
│       └── CallLogModal.jsx    # 架電記録モーダル
├── views/
│   └── pages/
│       ├── customers/index.blade.php  # 顧客一覧画面
│       └── call-logs/index.blade.php  # 架電記録画面
└── css/
    └── app.css                # Tailwind CSS
```

---

## LaravelとReactの連携構造

### 1. Viteによるビルド設定

**vite.config.js**
```javascript
export default defineConfig({
    plugins: [
        laravel({
            input: ['resources/css/app.css', 'resources/js/app.tsx'],
            refresh: true,  // ホットリロード有効化
        }),
        react(),  // React用プラグイン
    ],
});
```

**ポイント解説**：
- Viteは開発時の高速ビルドとHMR（ホットモジュールリプレイスメント）を提供
- `laravel()`プラグインがLaravelとの統合を担当
- `react()`プラグインがJSX変換とReact固有の最適化を実行

### 2. BladeテンプレートからReactを呼び出す仕組み

**resources/views/layouts/app.blade.php**
```blade
<!DOCTYPE html>
<html>
<head>
    <!-- React用のリフレッシュ機能 -->
    @viteReactRefresh
    
    <!-- CSSとJavaScriptの読み込み -->
    @vite(['resources/css/app.css', 'resources/js/app.tsx'])
</head>
<body>
    <!-- コンテンツ -->
</body>
</html>
```

**なぜ@viteReactRefreshが必要？**
- React Fast Refreshを有効にして、開発時にコンポーネントの状態を保持したまま更新
- これがないと「preamble detection error」が発生

### 3. ReactコンポーネントのマウントポイントReactモーダルを配置したい場所にマウントポイントを設置：

**customers/index.blade.php**
```blade
<!-- 通常のHTML -->
<div class="container">
    <!-- テーブルなど -->
</div>

<!-- Reactコンポーネントがここにマウントされる -->
<div id="customer-modal-root"></div>
```

---

## コンポーネント詳細解説

### CustomerModal.jsx - 顧客登録モーダル

```jsx
import React, { useState } from 'react';

const CustomerModal = () => {
    // ========== STATE管理 ==========
    // モーダルの開閉状態
    const [isOpen, setIsOpen] = useState(false);
    
    // フォームデータ（すべての入力値を1つのオブジェクトで管理）
    const [formData, setFormData] = useState({
        company_name: '',
        contact_name: '',
        email: '',
        phone: '',
        industry: '',
        area: '',
        temperature_rating: '',
        priority: '3',      // デフォルト値
        status: 'new',      // デフォルト値
        memo: ''
    });
    
    // バリデーションエラー
    const [errors, setErrors] = useState({});
    
    // 送信中フラグ（二重送信防止）
    const [isSubmitting, setIsSubmitting] = useState(false);
```

**State設計の意図**：
1. **isOpen**: モーダルの表示/非表示を制御
2. **formData**: 全フィールドを1つのオブジェクトで管理することで、更新処理を簡潔に
3. **errors**: LaravelのバリデーションエラーをフィールドごとにObject形式で保持
4. **isSubmitting**: 送信ボタンの無効化と「送信中...」表示に使用

### イベントハンドラの仕組み

```jsx
// 入力値変更時の処理
const handleChange = (e) => {
    const { name, value } = e.target;
    setFormData(prev => ({
        ...prev,        // 既存の値を展開
        [name]: value   // 該当フィールドのみ更新
    }));
    
    // 入力時にエラーをクリア（UX改善）
    if (errors[name]) {
        setErrors(prev => ({
            ...prev,
            [name]: ''
        }));
    }
};
```

**なぜこの書き方？**
- `...prev`（スプレッド演算子）で既存のstateをコピー
- `[name]: value`で動的にプロパティを更新
- これによりすべてのinputに同じハンドラを使い回せる

### Ajax通信の実装

```jsx
const handleSubmit = async (e) => {
    e.preventDefault();  // フォームのデフォルト送信を防ぐ
    setIsSubmitting(true);
    setErrors({});
    
    try {
        // CSRFトークンを取得（Laravel必須）
        const token = document.querySelector('meta[name="csrf-token"]')?.getAttribute('content');
        
        const response = await fetch('/customers', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'X-CSRF-TOKEN': token,  // Laravel CSRF保護
                'X-Requested-With': 'XMLHttpRequest',  // Ajax判定用
                'Accept': 'application/json',
            },
            body: JSON.stringify(formData)
        });
        
        const data = await response.json();
        
        if (!response.ok) {
            // バリデーションエラー処理
            if (response.status === 422 && data.errors) {
                setErrors(data.errors);
            } else {
                throw new Error(data.message || 'エラーが発生しました');
            }
            return;
        }
        
        // 成功時の処理
        window.location.href = `/customers/${data.customer.id}`;
        
    } catch (error) {
        console.error('Error:', error);
        alert(error.message || 'エラーが発生しました');
    } finally {
        setIsSubmitting(false);  // 必ず実行
    }
};
```

**重要ポイント**：
1. **CSRF Token**: LaravelのセキュリティのためH必須
2. **X-Requested-With**: LaravelがAjaxリクエストと判定するために必要
3. **422ステータス**: Laravelのバリデーションエラー規約
4. **finally節**: エラーでも成功でも必ずisSubmittingをfalseに

### useEffectの活用

```jsx
useEffect(() => {
    // グローバル関数として露出（Blade側から呼び出し可能にする）
    window.openCustomerModal = () => setIsOpen(true);
    
    // クリーンアップ（コンポーネント破棄時）
    return () => {
        delete window.openCustomerModal;
    };
}, []);  // 空配列 = マウント時のみ実行
```

**なぜwindowオブジェクトに関数を追加？**
- Blade側の既存のボタンからReactモーダルを開くため
- グローバルスコープに関数を配置することで、どこからでも呼び出し可能

### CallLogModal.jsx - 架電記録モーダル

顧客モーダルとの**主な違い**：

```jsx
// 顧客データの取得
const [customers, setCustomers] = useState([]);
const [isLoadingCustomers, setIsLoadingCustomers] = useState(false);

useEffect(() => {
    if (isOpen) {
        fetchCustomers();  // モーダルを開いたときに顧客リストを取得
    }
}, [isOpen]);  // isOpenが変更されるたびに実行

const fetchCustomers = async () => {
    setIsLoadingCustomers(true);
    try {
        const response = await fetch('/api/customers');
        const data = await response.json();
        setCustomers(data.customers || []);
    } catch (error) {
        console.error('Failed to fetch customers:', error);
    } finally {
        setIsLoadingCustomers(false);
    }
};
```

**設計の意図**：
- 顧客リストは動的に変わるため、モーダルを開くたびに最新データを取得
- ローディング状態を管理してUXを向上

---

## 重要な実装パターン

### 1. Reactコンポーネントの初期化パターン

**app.tsx**
```tsx
import React from 'react';
import { createRoot } from 'react-dom/client';
import CustomerModal from './components/CustomerModal';
import CallLogModal from './components/CallLogModal';

// DOMContentLoadedで確実にDOM構築後に実行
if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initializeReactComponents);
} else {
    initializeReactComponents();
}

function initializeReactComponents() {
    // 顧客モーダルのマウント
    const customerModalContainer = document.getElementById('customer-modal-root');
    if (customerModalContainer) {
        const root = createRoot(customerModalContainer);
        root.render(<CustomerModal />);
    }
    
    // 架電モーダルのマウント
    const callLogModalContainer = document.getElementById('call-log-modal-root');
    if (callLogModalContainer) {
        const root = createRoot(callLogModalContainer);
        root.render(<CallLogModal />);
    }
}
```

**なぜこの実装？**
- React 18の新しいcreateRoot APIを使用
- 各ページで必要なコンポーネントのみマウント（パフォーマンス最適化）
- DOMContentLoadedで確実な初期化を保証

### 2. 条件付きレンダリングパターン

```jsx
return (
    <>
        {/* ボタンは常に表示 */}
        <button onClick={() => setIsOpen(true)}>
            新規作成
        </button>
        
        {/* モーダルは isOpen が true の時のみレンダリング */}
        {isOpen && (
            <div className="modal-overlay" onClick={handleClose}>
                <div className="modal-content" onClick={(e) => e.stopPropagation()}>
                    {/* モーダル内容 */}
                </div>
            </div>
        )}
    </>
);
```

**ポイント**：
- `&&`演算子で条件付きレンダリング
- `stopPropagation()`でモーダル内部クリック時に閉じないようにする

### 3. フォームバリデーションパターン

```jsx
{/* エラー表示の共通パターン */}
<input
    type="text"
    name="company_name"
    value={formData.company_name}
    onChange={handleChange}
    className={`form-input ${errors.company_name ? 'border-red-500' : ''}`}
/>
{errors.company_name && (
    <p className="text-red-500 text-sm mt-1">
        {errors.company_name[0]}  {/* Laravelは配列で返す */}
    </p>
)}
```

---

## トラブルシューティングから学んだこと

### 1. Vite Preamble Error

**エラー**：
```
@vitejs/plugin-react can't detect preamble. Something is wrong.
```

**原因と解決**：
1. `@viteReactRefresh`がBlade側に不足
2. TSXファイルの型エラー
3. 解決策：JSXに変更 + @viteReactRefresh追加

### 2. CSRFトークンエラー

**エラー**：
```
419 Page Expired
```

**解決**：
```jsx
// metaタグからトークンを取得
const token = document.querySelector('meta[name="csrf-token"]')?.getAttribute('content');

// ヘッダーに含める
headers: {
    'X-CSRF-TOKEN': token,
}
```

### 3. State更新の非同期性

**注意点**：
```jsx
// ❌ 間違い：即座には反映されない
setFormData({...formData, name: value});
console.log(formData.name);  // 古い値が出力される

// ✅ 正しい：useEffectで監視
useEffect(() => {
    console.log('formData updated:', formData);
}, [formData]);
```

---

## 今後の学習ポイント

### 次に学ぶべきReact概念

1. **useReducer**: 複雑なstate管理に有効
2. **useCallback/useMemo**: パフォーマンス最適化
3. **Context API**: グローバルstate管理
4. **カスタムフック**: ロジックの再利用

### Laravel + Reactの発展形

1. **Inertia.js**: LaravelとReactをより密に統合
2. **Laravel Sanctum**: API認証の実装
3. **React Router**: SPA化
4. **Redux/Zustand**: 大規模state管理

---

## まとめ

今回の実装で重要な点：

1. **LaravelとReactの役割分担**
   - Laravel: データ処理、バリデーション、API
   - React: 動的UI、ユーザーインタラクション

2. **段階的な導入**
   - 全面React化ではなく、必要な部分のみReact化
   - 既存のBlade資産を活かしながら modernize

3. **エラーハンドリングの重要性**
   - バリデーションエラーの適切な表示
   - ローディング状態の管理
   - 二重送信の防止

この実装パターンを理解すれば、今後の機能追加（ダッシュボード、グラフ表示など）にも応用できます。