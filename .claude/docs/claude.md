# Claude 連携ガイド（TellSupo開発）

最終更新: 2025-11-04  
対象: TellSupo（テルサポ）開発チーム

## 🎯 目的

このドキュメントは、TellSupoプロジェクトにおけるClaude（AI）との効果的な協業方法を定義します。

## 📋 基本方針

### 役割分担
- **開発者**: 仕様策定、レビュー、ビジネスロジック判断
- **Claude**: 実装、テスト作成、コード最適化、ドキュメント作成

### 品質保証
- PSR-12準拠
- Laravel Pint適用
- 全機能にFeatureテスト
- N+1クエリ禁止

## 🛠️ セットアップ手順

### 1. プロジェクト情報の共有

Claude利用時は必ず以下の前提を伝える：

```
前提条件:
- Laravel 11.x / PHP 8.2 / MySQL 8.0
- Blade Templates + Tailwind CSS + Alpine.js
- 日本時間（JST）基準
- Controller薄く、Service層で業務ロジック
- N+1クエリ禁止（with句必須）
- 全てFeatureテストでカバー
```

### 2. 出力形式の指定

```
出力順序:
1. 既存ファイルの差分（Edit）
2. 新規ファイル（Write）
3. 実行コマンド（migration、route等）
4. テストコード
5. 自己チェックリスト

疑似コードは禁止。実行可能なコードのみ。
```

## 📝 依頼パターン

### パターン1: CRUD実装
```
依頼例:
F007-03: CustomerRequest（Store/Update）を実装。
- 電話番号のハイフン自動除去
- unique検証（Updateは自分以外）
- required: name, phone, email
- optional: address, notes
- エラーメッセージは日本語

出力順: 差分→新規→コマンド→テスト→チェック
```

### パターン2: UI実装
```
依頼例:
F005-04: 顧客一覧画面を実装。
- paginate(20)、検索フォーム付き
- 空状態の表示
- Tailwind使用、モバイル対応
- 新規登録ボタン配置

出力順: 差分→新規→コマンド→テスト→チェック
```

### パターン3: 機能テスト
```
依頼例:
F007-06: 顧客CRUDのFeatureテストを作成。
- 正常系: 登録/更新/削除
- 異常系: 重複電話番号、未入力
- 境界値: 最大文字数

出力順: 差分→新規→コマンド→テスト→チェック
```

## 🔄 ワークフロー

### 1. ブランチ準備
```bash
git checkout -b feat/f007-crud-03-formrequest
```

### 2. 作業指示書作成
```markdown
# CLAUDE_TASK.md
## 目的
F007-03: CustomerRequestの実装

## Scope
- In: app/Http/Requests/Customer/
- Out: Controller、Model は触らない

## DoD
- [ ] Pint差分0
- [ ] php artisan test 緑
- [ ] 電話番号正規化テスト通過
```

### 3. Claude実装依頼
上記パターンを参考に具体的に依頼

### 4. テスト実行
```bash
composer exec pint -- --test
php artisan test
```

### 5. PR作成
- `CLAUDE_TASK.md`内容をPR本文に転記
- `git rm CLAUDE_TASK.md`で削除
- スクリーンショット添付

## ⚠️ 注意事項

### 禁止事項
- 疑似コードでの回答
- N+1クエリを含む実装
- mainブランチでの`CLAUDE_TASK.md`残存
- テストなしでのPR作成
- BladeファイルへのJavaScript直接記述（必ず分離）

### 推奨事項
- 小さな単位での実装依頼
- 具体的な仕様の明示
- エラーケースの網羅的テスト
- UIのスクリーンショット確認
- JavaScript は public/js/ に分離し、Blade から @vite で読み込み

## 📚 関連ドキュメント

- `claude-coding-standards.md` - コーディング規約
- `claude-code-playbook.md` - 実装テンプレート集
- `AGENT_CHAT_INDEX.md` - チャット履歴管理
- `branch-chat-guide.md` - ブランチ別運用ガイド

## 🔧 トラブルシューティング

### よくある問題

**Q: N+1クエリが発生している**
```php
// ❌ NG
$customers = Customer::all();
foreach($customers as $customer) {
    echo $customer->callLogs->count(); // N+1発生
}

// ✅ OK
$customers = Customer::withCount('callLogs')->get();
foreach($customers as $customer) {
    echo $customer->call_logs_count;
}
```

**Q: テストが失敗する**
```bash
# データベースリフレッシュ
php artisan migrate:fresh --seed
php artisan test --filter CustomerTest
```

**Q: Pint エラー**
```bash
# 自動修正
composer exec pint
# 確認のみ
composer exec pint -- --test
```

## 📞 サポート

- 技術的質問: Issues作成
- 仕様確認: PRコメント
- 緊急事項: Slack #telsupo-dev