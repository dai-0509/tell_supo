<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default Hash Driver
    |--------------------------------------------------------------------------
    |
    | サポート: "bcrypt", "argon", "argon2id"
    | .env の HASH_DRIVER で上書きできます（例: HASH_DRIVER=argon2id）
    |
    */

    'driver' => env('HASH_DRIVER', 'bcrypt'),

    /*
    |--------------------------------------------------------------------------
    | Bcrypt Options
    |--------------------------------------------------------------------------
    */

    'bcrypt' => [
        'rounds' => env('BCRYPT_ROUNDS', 10),
    ],

    /*
    |--------------------------------------------------------------------------
    | Argon / Argon2id Options
    |--------------------------------------------------------------------------
    |
    | Argon/Argon2id に共通のパラメータです。
    | メモリ・時間・スレッドは本番負荷に応じて調整してください。
    |
    */

    'argon' => [
        'memory'  => env('ARGON_MEMORY', 65536), // KB
        'threads' => env('ARGON_THREADS', 1),
        'time'    => env('ARGON_TIME', 4),
    ],

];
